; Graph plugin configuration
; Stores email stats in SQLite database for historical graphs
; View at: http://YOUR_IP:8080/graph

db_file = /tmp/haraka_graph.db
http_addr = 0.0.0.0
http_port = 8080
ignore_re = queue|graph|relay|watch
