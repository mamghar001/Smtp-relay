; Haraka HTTP server configuration
; Required for graph and watch web interfaces

; IP address and port to listen on
listen = 0.0.0.0:8080

; Document root for static files
docroot = /etc/haraka/html
